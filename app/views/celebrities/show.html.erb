<%= @celebrity.first_name %>
<%= @celebrity.bio %>
<%= link_to "Book #{@celebrity.first_name}", new_celebrity_booking_path(@celebrity), class:"btn btn-outline-primary"%>

<div class="col-12 col-sm-6">
  <div class="bg-white rounded p-3">
    <h3>Book <em><b><%= @celebrity.first_name %> <%= @celebrity.last_name %></b></em> right now! </h3>
    <%= render partial: "form" %>
  </div>
</div>
